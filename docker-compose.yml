services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./prisma:/app/prisma
      - ./public/uploads:/app/public/uploads
      - ./logs:/app/logs
    env_file:
      - .env
    restart: unless-stopped
